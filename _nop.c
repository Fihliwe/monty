#include "monty.h"
/**
  * _nop -  doesn’t do anything. 
  * @head: stack head
  * @line_number: error message
  * Return: nothing
 */

void f_nop(stack_t **head, unsigned int line_number)
{
	(void) line_number;
	(void) head;
}
